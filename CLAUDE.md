# 项目约定

## 命令
- 构建: cd ${PROJECT_ROOT}/frontend && npm install && npm run build
- 启动后端: pkill -f "flask run" && cd ${PROJECT_ROOT}/backend && ./start.sh &
- 启动前端: cd ${PROJECT_ROOT}/frontend && npm run dev &

## 项目进展
- 查看项目中的《功能实现状态.md》

## 要求
- 每完成一个特性的开发后需要更新《功能实现状态.md》